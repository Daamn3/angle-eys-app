import 'package:flutter/material.dart';

class ct_location extends StatefulWidget {
  const ct_location({super.key});

  @override
  State<ct_location> createState() => _ct_locationState();
}

class _ct_locationState extends State<ct_location> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ct_location'),),
      body: Center(
        child: SingleChildScrollView(
          child: Column(
            children: [

            ],
          ),
        ),
      ),
    );
  }
}
