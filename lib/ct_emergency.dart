import 'package:flutter/material.dart';

class ct_emergency extends StatefulWidget {
  const ct_emergency({super.key});

  @override
  State<ct_emergency> createState() => _ct_emergencyState();
}

class _ct_emergencyState extends State<ct_emergency> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ct_emergency'),),
      body: Center(
        child: SingleChildScrollView(
          child: Column(
            children: [

            ],
          ),
        ),
      ),
    );
  }
}
