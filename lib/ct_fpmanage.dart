import 'package:flutter/material.dart';

class ct_fpmanage extends StatefulWidget {
  const ct_fpmanage({super.key});

  @override
  State<ct_fpmanage> createState() => _ct_fpmanageState();
}

class _ct_fpmanageState extends State<ct_fpmanage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ct_fpmanage'),),
      body: Center(
        child: SingleChildScrollView(
          child: Column(
            children: [

            ],
          ),
        ),
      ),
    );
  }
}
