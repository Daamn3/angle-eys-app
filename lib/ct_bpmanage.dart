import 'package:flutter/material.dart';

class ct_bpmanage extends StatefulWidget {
  const ct_bpmanage({super.key});

  @override
  State<ct_bpmanage> createState() => _ct_bpmanageState();
}

class _ct_bpmanageState extends State<ct_bpmanage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ct_bpmanage'),),
      body: Center(
        child: SingleChildScrollView(
          child: Column(
            children: [

            ],
          ),
        ),
      ),
    );
  }
}
